

<% if @response.errors.any? %>
<% @response.errors.full_messages.each do |message| %>
<div class="alert alert-dismissable alert-danger">
  <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
  <%= message %>
</div>
<% end %>
<% end %>

<!-- <div class="page-header">
  <h1>Add Response</h1>
</div> -->



<div class="container">
  <div class="row">
    <div class="col-md-12">
      <div class="lead">

        <h1 class="text-center">Turn Your Frown Upside Down </h1>
        <h2 class="text-center">There are no problems in life only solutions</h2>


      </div>



<form action="/create_response" class="form-horizontal">
      <!-- Label and input for text -->
       <div class="row">
          <p>Type of problem</p>

          <%= select_tag(:answer_id, options_from_collection_for_select(Answer.all, :id, :text, @user_id), :class => "form-control") %>

          <%= select_tag(:user_id, options_from_collection_for_select(User.all, :id, :firstname, @user_id), :class => "form-control") %>

        </div>


  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <input type="submit" value="Create Response" class="btn btn-primary">
    </div>
  </div>
</form>
</div>
</div>
