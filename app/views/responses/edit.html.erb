<% if @response.errors.any? %>
  <% @response.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h1>Edit Response #<%= @response.id %></h1>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_response/<%= @response.id %>" class="form-horizontal">
      <!-- Label and input for user_id -->
      <div class="form-group">
        <label for="user_id" class="col-sm-2 control-label">
          User
        </label>
        <div class="col-sm-10">
          <input type="text" id="user_id" name="user_id" placeholder="User" class="form-control" value="<%= @response.user_id %>">
        </div>
      </div>

      <!-- Label and input for answer_id -->
      <div class="form-group">
        <label for="answer_id" class="col-sm-2 control-label">
          Answer
        </label>
        <div class="col-sm-10">
          <input type="text" id="answer_id" name="answer_id" placeholder="Answer" class="form-control" value="<%= @response.answer_id %>">
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <input type="submit" value="Update Response" class="btn btn-primary">
        </div>
      </div>
    </form>
  </div>
</div>

<div class="row">
  <div class="col-md-offset-3 col-md-6">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Edit Response #<%= @response.id %></h3>
      </div>
      <div class="panel-body">
        <form action="/update_response/<%= @response.id %>" class="form-horizontal">
          <!-- Label and input for user_id -->
          <div class="form-group">
            <label for="user_id" class="col-sm-2 control-label">
              User
            </label>
            <div class="col-sm-10">
              <input type="text" id="user_id" name="user_id" placeholder="User" class="form-control" value="<%= @response.user_id %>">
            </div>
          </div>

          <!-- Label and input for answer_id -->
          <div class="form-group">
            <label for="answer_id" class="col-sm-2 control-label">
              Answer
            </label>
            <div class="col-sm-10">
              <input type="text" id="answer_id" name="answer_id" placeholder="Answer" class="form-control" value="<%= @response.answer_id %>">
            </div>
          </div>

          <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
              <input type="submit" value="Update Response" class="btn btn-primary">
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
